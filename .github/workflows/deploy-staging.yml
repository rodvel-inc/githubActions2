# .github/workflows/deploy-staging.yml
name: Auto-Deploy a Staging

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Rama a desplegar"
        required: true
        default: "develop"

jobs:
  simple-test:
    runs-on: [self-hosted]
    steps:
      - name: Confirm Dispatch
        run: echo "Workflow dispatched successfully for branch ${{ github.event.inputs.ref }}"